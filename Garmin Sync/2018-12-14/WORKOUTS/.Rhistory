# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
#
files[idxfit]
# determine sub-folder to search through
sf = subfolders[11]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
#
files[idxfit]
sf
# determine sub-folder to search through
sf = subfolders[11]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# LOCATION has a .FIT file with no biological, mechanical, or positional data
files[idxfit]
# determine sub-folder to search through
sf = subfolders[12]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# LOCATION has a .FIT file with no biological, mechanical, or positional data
files[idxfit]
sf
# determine sub-folder to search through
sf = subfolders[13]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# LOCATION, METRICS has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[13]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR
#
# LOCATION, METRICS has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
colnames(read.fit(files[1])$monitoring)
subfolders
# determine sub-folder to search through
sf = subfolders[14]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[15]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[16]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[17]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[18]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[19]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[20]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS has a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[21]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS, SLEEP have a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[22]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS, SLEEP have a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
idxfit
# determine sub-folder to search through
sf = subfolders[22]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS, SLEEP have a .FIT file with no biological, mechanical, or positional data
sf
files[idxfit]
sf
subfolders
# determine sub-folder to search through
sf = subfolders[23]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS, SLEEP, SPORTS have .FIT files with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[24]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS, SLEEP, SPORTS have .FIT files with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[25]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS, SLEEP, SPORTS have .FIT files with no biological, mechanical, or positional data
sf
files[idxfit]
# determine sub-folder to search through
sf = subfolders[26]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS, SLEEP, SPORTS have .FIT files with no biological, mechanical, or positional data
sf
files[idxfit]
subfolders
# determine sub-folder to search through
sf = subfolders[27]
# go to a sub-folder
setwd(paste(GarminSyncDate,"/",sf,sep=""))
# get the .FIT files
files = dir() # get .fit files in this folder
idxfit = which(endsWith(files,".FIT")); # indecies of .FIT files in directory
# read them and print contents to screen
files[idxfit]
for(fi in idxfit){
print(paste("File: ",fi,sep=""))
print(read.fit(files[fi]))
}
# Notes:
# ACTIVITY:
# file1$record includes; cadence, distance, heart rate, speed, timestamp
# file2$record includes; altitude, cadence, distance, heart rate, position_lat, position_long, speed, timestamp
# MONITOR (subfolders[13])
# file1$monitoring includes;
# "active_calories"                 "active_time"                     "activity_type"                   "current_activity_type_intensity"
# "cycles"                          "distance"                        "duration_min"                    "heart_rate"
# "intensity"                       "timestamp"                       "timestamp_16"
# file2 has a monitoring field, but didn't have any information
# LOCATION, METRICS. RECORDS, SCHEDULE, SETTINGS, SLEEP, SPORTS have .FIT files with no biological, mechanical, or positional data
sf
files[idxfit]
